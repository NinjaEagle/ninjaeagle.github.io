# Kevin Wang — portfolio

Personal site for [kevwang.me](https://www.kevwang.me).

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Deploy to GitHub Pages with `npm run deploy` (publishes the `dist` folder).
